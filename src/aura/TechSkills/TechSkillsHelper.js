({
	//if the batch main view is loaded, this function is called to display a radar chart
    batchRadar : function(component) {
        
        
        
    },
    //if the associate view is loaded, this function is called to display a radar chart for associates
    associateRadar : function(component) {
    
    
    
	},
 
 	//if the batch by week is loaded, this function is called to display QC results
 	batchDonut : function(component) {



	}
})